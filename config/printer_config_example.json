{
  "mqtt": {
    "url": "mqtt://127.0.0.1:1883",
    "topic": "zebra/print",
    "username": null,
    "password": null,
    "_comment": {
      "url": "MQTT连接URL，格式: protocol://host:port（不包含topic）",
      "topic": "MQTT订阅主题，独立配置",
      "示例URL": [
        "mqtt://127.0.0.1:1883",
        "ws://10.100.10.121:8083/mqtt",
        "tcp://10.100.10.121:1883",
        "mqtts://example.com:8883"
      ],
      "说明": "URL中的路径部分（如 /mqtt）会保留但不会用作topic，topic需要单独配置。也可以使用分离格式：host、port、topic分别配置（向后兼容）"
    }
  },
  "printers": [
    {
      "name": "ZT411",
      "types": ["label"],
      "ip": null,
      "port": 9100,
      "device": null,
      "default": true,
      "_comment": "斑马标签打印机 - 处理ZPL标签"
    },
    {
      "name": "HP LaserJet",
      "types": ["pdf"],
      "ip": null,
      "port": 9100,
      "device": null,
      "_comment": "激光打印机 - 处理PDF文档"
    },
    {
      "name": "热敏打印机",
      "types": ["receipt", "escpos"],
      "ip": "192.168.1.100",
      "port": 9100,
      "device": null,
      "_comment": "ESC/POS热敏打印机 - 处理小票"
    }
  ],
  "_comment": {
    "printers": "打印机配置数组",
    "name": "打印机名称（支持模糊搜索）",
    "types": "支持的打印类型: ['label', 'pdf', 'receipt'] 或 ['*'] 表示全部",
    "ip": "网络打印机IP地址",
    "port": "网络打印端口（默认9100）",
    "device": "Linux设备路径（如/dev/usb/lp0）",
    "default": "是否为默认打印机（找不到匹配时使用）"
  },
  "_examples": {
    "单打印机配置（兼容旧版）": {
      "printer": {
        "name": "ZT411",
        "ip": null
      }
    },
    "多打印机配置（推荐）": {
      "printers": [
        {"name": "ZT411", "types": ["label"], "default": true},
        {"name": "HP", "types": ["pdf"]},
        {"name": "热敏", "types": ["receipt"]}
      ]
    },
    "通用打印机": {
      "printers": [
        {"name": "Universal", "types": ["*"], "default": true}
      ]
    }
  },
  "_upgrade_notes": {
    "v2.0": "支持多打印机配置，使用 'printers' 数组替代单个 'printer' 对象",
    "兼容性": "仍然兼容旧版 'printer' 配置，新旧配置可以同时存在",
    "types字段": "必须使用数组格式 ['label', 'pdf', 'receipt']，不再支持 'print_type' 单值字段"
  }
}

